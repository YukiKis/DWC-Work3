<section class="books-edit">
  <h2>Editting Book</h2>
  <%= form_with model: @book, local: true do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :body %>
    <%= f.text_area :body %>
    <%= f.submit "Update Book" %>
  <% end %>
</section>